spring.application.name=backend

# PostgreSQL config (Render)
# URL/username can be overridden in Render environment variables.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d7iqt9t8nd3s738leck0-a:5432/task_list_0hyi?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:task_list_0hyi_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

# JPA config
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true